name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Syntax-check all modules
        run: |
          for f in js/*.js js/ui/*.js; do node --check "$f"; done
      - name: Run engine unit tests
        run: node --test tests/*.test.mjs
